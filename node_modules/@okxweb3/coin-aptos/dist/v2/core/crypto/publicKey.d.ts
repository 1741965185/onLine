import { Serializable } from "../../bcs";
import { HexInput } from "../../types";
import { AuthenticationKey } from "../authenticationKey";
import { Signature } from "./signature";
export interface VerifySignatureArgs {
    message: HexInput;
    signature: Signature;
}
export declare abstract class PublicKey extends Serializable {
    abstract verifySignature(args: VerifySignatureArgs): boolean;
    abstract toUint8Array(): Uint8Array;
    toString(): string;
}
export declare abstract class AccountPublicKey extends PublicKey {
    abstract authKey(): AuthenticationKey;
}
