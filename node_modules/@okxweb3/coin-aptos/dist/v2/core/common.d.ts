export declare class ParsingError<T> extends Error {
    invalidReason: T;
    constructor(message: string, invalidReason: T);
}
export type ParsingResult<T> = {
    valid: boolean;
    invalidReason?: T;
    invalidReasonMessage?: string;
};
