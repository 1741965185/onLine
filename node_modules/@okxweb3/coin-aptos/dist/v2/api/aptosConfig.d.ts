import { Network } from "../utils/apiEndpoints";
import { AptosSettings } from "../types";
export declare class AptosConfig {
    readonly network: Network;
    readonly moveModule?: string;
    constructor(settings?: AptosSettings);
}
